{
  "name": "nerd-utility-mcp",
  "version": "1.1.0",
  "description": "MCP server exposing the Nerd Utility API (60 endpoints: format, validators, decoders, converters, cron, TOTP, duration, units)",
  "main": "src/mcp-server.js",
  "bin": { "nerd-utility-mcp": "src/mcp-server.js" },
  "scripts": { "start": "node src/mcp-server.js" },
  "keywords": ["mcp", "utility", "api", "validator", "model-context-protocol"],
  "license": "MIT",
  "engines": { "node": ">=18" }
}
